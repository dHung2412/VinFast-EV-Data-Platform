name: vinfast
version: '1.0'
config-version: 2

profile: vinfast

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  vinfast:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
